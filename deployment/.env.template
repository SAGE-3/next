SAGE3_SERVER=
TOKEN=
NODE_ENV=production
CHROMA_SERVER_AUTHN_CREDENTIALS='admin:$2y$05$wGPLsb5jjBXItIZBRSqoguNJAb1wWxTba4bL20PcyRG5.iPG6/zm6'
CHROMA_CLIENT_AUTH_CREDENTIALS=admin:sage123
NODE_SERVER_REPLICAS=2

# Screenshare (self-hosted LiveKit SFU).
# Generate one value and screensharing turns on; leave it empty and the server
# offers no screenshare at all. Must be at least 32 characters -- `uuidgen` gives 36:
#   LIVEKIT_API_SECRET=$(uuidgen)
# Requires 7882/udp and 7881/tcp open on this host (forwarded, if behind NAT).
LIVEKIT_API_SECRET=
NODE_FILE_SERVER_REPLICAS=2
